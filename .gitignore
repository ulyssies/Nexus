node_modules/
dist/
.DS_Store

# secrets & local db (never commit)
.env
server/.env
server/db/nexus.db
server/db/nexus.db-shm
server/db/nexus.db-wal
credentials.json
server/credentials.json
token.json
server/gmail-token.json

# personal data — never publish (your local copies stay; they're just not committed)
.claude/settings.local.json
server/resumes/*.tex
server/resumes/*.pdf
